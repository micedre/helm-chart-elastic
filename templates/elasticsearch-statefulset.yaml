apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "elasticsearch.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "elasticsearch.labels" . | nindent 4 }}
    app.kubernetes.io/component: elasticsearch
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  serviceName: {{ include "elasticsearch.masterService" . }}
  replicas: {{ .Values.elasticsearch.replicas }}
  updateStrategy:
    type: {{ .Values.elasticsearch.updateStrategy.type | default "RollingUpdate" }}
    {{- if eq (.Values.elasticsearch.updateStrategy.type | default "RollingUpdate") "RollingUpdate" }}
    rollingUpdate:
      partition: {{ .Values.elasticsearch.updateStrategy.rollingUpdate.partition | default 0 }}
    {{- end }}
  selector:
    matchLabels:
      {{- include "elasticsearch.esSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/elasticsearch-configmap.yaml") . | sha256sum }}
        {{- with .Values.elasticsearch.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "elasticsearch.esSelectorLabels" . | nindent 8 }}
    spec:
      {{- if .Values.elasticsearch.serviceAccount.create }}
      serviceAccountName: {{ include "elasticsearch.serviceAccountName" . }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.elasticsearch.podSecurityContext | nindent 8 }}
      terminationGracePeriodSeconds: {{ .Values.elasticsearch.terminationGracePeriodSeconds | default 120 }}
      initContainers:
      # # Increase vm.max_map_count to 262144 for Elasticsearch
      # - name: configure-sysctl
      #   image: busybox:1.36
      #   command: ['sh', '-c', 'sysctl -w vm.max_map_count=262144']
      #   securityContext:
      #     privileged: true
      #     runAsUser: 0
      {{- if and .Values.elasticsearch.security.enabled .Values.elasticsearch.security.tls.enabled .Values.elasticsearch.security.tls.useSelfSignedCerts }}
      # Generate self-signed certificates
      - name: generate-certs
        image: {{ if .Values.elasticsearch.image.custom.enabled }}{{ .Values.elasticsearch.image.custom.repository }}:{{ .Values.elasticsearch.image.custom.tag }}{{ else }}{{ .Values.elasticsearch.image.repository }}:{{ .Values.elasticsearch.image.tag }}{{ end }}
        command: ['sh', '-c']
        args:
        - |
          if [ ! -f /usr/share/elasticsearch/config/certs/elastic-certificates.p12 ]; then
            bin/elasticsearch-certutil cert --silent --pem --in /tmp/instances.yml -out /tmp/certs.zip
            unzip /tmp/certs.zip -d /usr/share/elasticsearch/config/certs
            chmod -R 755 /usr/share/elasticsearch/config/certs
          fi
        {{- with .Values.elasticsearch.initContainers.resources }}
        resources:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        volumeMounts:
        - name: elastic-certificates
          mountPath: /usr/share/elasticsearch/config/certs
        - name: cert-config
          mountPath: /tmp/instances.yml
          subPath: instances.yml
      {{- end }}
      containers:
      - name: elasticsearch
        image: {{ if .Values.elasticsearch.image.custom.enabled }}{{ .Values.elasticsearch.image.custom.repository }}:{{ .Values.elasticsearch.image.custom.tag }}{{ else }}{{ .Values.elasticsearch.image.repository }}:{{ .Values.elasticsearch.image.tag }}{{ end }}
        imagePullPolicy: {{ .Values.elasticsearch.image.pullPolicy }}
        securityContext:
          {{- toYaml .Values.elasticsearch.securityContext | nindent 10 }}
        ports:
        - name: http
          containerPort: 9200
          protocol: TCP
        - name: transport
          containerPort: 9300
          protocol: TCP
        env:
        - name: node.name
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: cluster.name
          value: {{ include "elasticsearch.clusterName" . }}
        {{- if .Values.elasticsearch.clusterMode }}
        - name: discovery.seed_hosts
          value: {{ include "elasticsearch.masterService" . }}
        - name: cluster.initial_master_nodes
          value: {{ include "elasticsearch.fullname" . }}-0,{{ include "elasticsearch.fullname" . }}-1,{{ include "elasticsearch.fullname" . }}-2
        {{- else }}
        - name: discovery.type
          value: single-node
        {{- end }}
        - name: ES_JAVA_OPTS
          value: {{ .Values.elasticsearch.javaOpts | quote }}
        - name: network.host
          value: "0.0.0.0"
        {{- if .Values.elasticsearch.security.enabled }}
        - name: xpack.security.enabled
          value: "true"
        {{- if .Values.elasticsearch.security.tls.enabled }}
        - name: xpack.security.transport.ssl.enabled
          value: "true"
        - name: xpack.security.transport.ssl.verification_mode
          value: "certificate"
        - name: xpack.security.transport.ssl.keystore.path
          value: "/usr/share/elasticsearch/config/certs/elastic-certificates.p12"
        - name: xpack.security.transport.ssl.truststore.path
          value: "/usr/share/elasticsearch/config/certs/elastic-certificates.p12"
        - name: xpack.security.http.ssl.enabled
          value: "true"
        - name: xpack.security.http.ssl.keystore.path
          value: "/usr/share/elasticsearch/config/certs/elastic-certificates.p12"
        {{- end }}
        - name: ELASTIC_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ include "elasticsearch.fullname" . }}-credentials
              key: password
        {{- else }}
        - name: xpack.security.enabled
          value: "false"
        {{- end }}
        {{- with .Values.elasticsearch.extraEnvVars }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        resources:
          {{- toYaml .Values.elasticsearch.resources | nindent 10 }}
        volumeMounts:
        - name: data
          mountPath: /usr/share/elasticsearch/data
        - name: config
          mountPath: /usr/share/elasticsearch/config/elasticsearch.yml
          subPath: elasticsearch.yml
        {{- if and .Values.elasticsearch.security.enabled .Values.elasticsearch.security.tls.enabled }}
        - name: elastic-certificates
          mountPath: /usr/share/elasticsearch/config/certs
        {{- end }}
        {{- with .Values.elasticsearch.extraVolumeMounts }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- if .Values.elasticsearch.livenessProbe.enabled }}
        livenessProbe:
          tcpSocket:
            port: 9300
          initialDelaySeconds: {{ .Values.elasticsearch.livenessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.elasticsearch.livenessProbe.periodSeconds }}
          timeoutSeconds: {{ .Values.elasticsearch.livenessProbe.timeoutSeconds }}
          failureThreshold: {{ .Values.elasticsearch.livenessProbe.failureThreshold }}
          successThreshold: {{ .Values.elasticsearch.livenessProbe.successThreshold }}
        {{- end }}
        {{- if .Values.elasticsearch.readinessProbe.enabled }}
        readinessProbe:
          {{- if .Values.elasticsearch.security.enabled }}
          exec:
            command:
            - sh
            - -c
            - |
              curl -sf -u "elastic:${ELASTIC_PASSWORD}" \
              {{- if .Values.elasticsearch.security.tls.enabled }}
              --cacert /usr/share/elasticsearch/config/certs/ca/ca.crt \
              https://localhost:9200/_cluster/health?local=true
              {{- else }}
              http://localhost:9200/_cluster/health?local=true
              {{- end }}
          {{- else }}
          httpGet:
            path: /_cluster/health?local=true
            port: 9200
            scheme: HTTP
          {{- end }}
          initialDelaySeconds: {{ .Values.elasticsearch.readinessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.elasticsearch.readinessProbe.periodSeconds }}
          timeoutSeconds: {{ .Values.elasticsearch.readinessProbe.timeoutSeconds }}
          failureThreshold: {{ .Values.elasticsearch.readinessProbe.failureThreshold }}
          successThreshold: {{ .Values.elasticsearch.readinessProbe.successThreshold }}
        {{- end }}
        {{- if .Values.elasticsearch.lifecycle.preStop.enabled }}
        lifecycle:
          preStop:
            exec:
              command:
              {{- if .Values.elasticsearch.lifecycle.preStop.command }}
              {{- toYaml .Values.elasticsearch.lifecycle.preStop.command | nindent 14 }}
              {{- else }}
              - /bin/sh
              - -c
              - |
                # Graceful shutdown: exclude node from voting config
                {{- if .Values.elasticsearch.security.enabled }}
                curl -sf -u "elastic:${ELASTIC_PASSWORD}" \
                  {{- if .Values.elasticsearch.security.tls.enabled }}
                  --cacert /usr/share/elasticsearch/config/certs/ca/ca.crt \
                  -X POST "https://localhost:9200/_cluster/voting_config_exclusions?node_names=${HOSTNAME}&timeout=1m"
                  {{- else }}
                  -X POST "http://localhost:9200/_cluster/voting_config_exclusions?node_names=${HOSTNAME}&timeout=1m"
                  {{- end }}
                {{- else }}
                curl -sf -X POST "http://localhost:9200/_cluster/voting_config_exclusions?node_names=${HOSTNAME}&timeout=1m"
                {{- end }}
                sleep 30
              {{- end }}
        {{- end }}
      volumes:
      - name: config
        configMap:
          name: {{ include "elasticsearch.fullname" . }}-config
      {{- if and .Values.elasticsearch.security.enabled .Values.elasticsearch.security.tls.enabled }}
      - name: elastic-certificates
        {{- if .Values.elasticsearch.security.tls.certificatesSecret }}
        secret:
          secretName: {{ .Values.elasticsearch.security.tls.certificatesSecret }}
        {{- else }}
        emptyDir: {}
        {{- end }}
      - name: cert-config
        configMap:
          name: {{ include "elasticsearch.fullname" . }}-cert-config
      {{- end }}
      {{- with .Values.elasticsearch.extraVolumes }}
      {{- toYaml . | nindent 6 }}
      {{- end }}
      {{- with .Values.elasticsearch.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.elasticsearch.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.elasticsearch.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.elasticsearch.affinity.podAntiAffinity }}
      affinity:
        podAntiAffinity:
          {{- if eq .Values.elasticsearch.affinity.podAntiAffinity "hard" }}
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchLabels:
                {{- include "elasticsearch.esSelectorLabels" . | nindent 16 }}
            topologyKey: kubernetes.io/hostname
          {{- else }}
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchLabels:
                  {{- include "elasticsearch.esSelectorLabels" . | nindent 18 }}
              topologyKey: kubernetes.io/hostname
          {{- end }}
      {{- end }}
  {{- if .Values.elasticsearch.persistence.enabled }}
  volumeClaimTemplates:
  - metadata:
      name: data
      labels:
        {{- include "elasticsearch.labels" . | nindent 8 }}
    spec:
      accessModes:
      - {{ .Values.elasticsearch.persistence.accessMode }}
      {{- if .Values.elasticsearch.persistence.storageClass }}
      storageClassName: {{ .Values.elasticsearch.persistence.storageClass }}
      {{- end }}
      resources:
        requests:
          storage: {{ .Values.elasticsearch.persistence.size }}
  {{- else }}
  - metadata:
      name: data
    spec:
      emptyDir: {}
  {{- end }}

{{- if .Values.kibana.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kibana.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kibana.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.kibana.replicas }}
  selector:
    matchLabels:
      {{- include "kibana.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/kibana-configmap.yaml") . | sha256sum }}
        {{- with .Values.kibana.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "kibana.selectorLabels" . | nindent 8 }}
    spec:
      securityContext:
        {{- toYaml .Values.kibana.podSecurityContext | nindent 8 }}
      initContainers:
      # Wait for Elasticsearch to be ready
      - name: wait-for-elasticsearch
        image: busybox:1.36
        command: ['sh', '-c']
        args:
        - |
          until nc -z {{ include "elasticsearch.fullname" . }} {{ .Values.elasticsearch.service.httpPort }}; do
            echo "Waiting for Elasticsearch..."
            sleep 5
          done
          echo "Elasticsearch is ready!"
      containers:
      - name: kibana
        image: {{ .Values.kibana.image.repository }}:{{ .Values.kibana.image.tag }}
        imagePullPolicy: {{ .Values.kibana.image.pullPolicy }}
        securityContext:
          {{- toYaml .Values.kibana.securityContext | nindent 10 }}
        ports:
        - name: http
          containerPort: 5601
          protocol: TCP
        env:
        - name: SERVER_HOST
          value: "0.0.0.0"
        - name: SERVER_NAME
          value: {{ include "kibana.fullname" . }}
        - name: ELASTICSEARCH_HOSTS
          {{- if and .Values.elasticsearch.security.enabled .Values.elasticsearch.security.tls.enabled }}
          value: "https://{{ include "elasticsearch.fullname" . }}:{{ .Values.elasticsearch.service.httpPort }}"
        - name: ELASTICSEARCH_SSL_VERIFICATIONMODE
          value: "none"
          {{- else }}
          value: "http://{{ include "elasticsearch.fullname" . }}:{{ .Values.elasticsearch.service.httpPort }}"
          {{- end }}
        {{- if .Values.elasticsearch.security.enabled }}
        - name: ELASTICSEARCH_USERNAME
          value: "elastic"
        - name: ELASTICSEARCH_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ include "elasticsearch.fullname" . }}-credentials
              key: password
        {{- end }}
        {{- with .Values.kibana.extraEnvVars }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        resources:
          {{- toYaml .Values.kibana.resources | nindent 10 }}
        volumeMounts:
        - name: config
          mountPath: /usr/share/kibana/config/kibana.yml
          subPath: kibana.yml
        livenessProbe:
          httpGet:
            path: /api/status
            port: 5601
          initialDelaySeconds: 120
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /api/status
            port: 5601
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
      volumes:
      - name: config
        configMap:
          name: {{ include "kibana.fullname" . }}-config
      {{- with .Values.kibana.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.kibana.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.kibana.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
